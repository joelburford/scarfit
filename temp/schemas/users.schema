CREATE TABLE users (
	id SERIAL PRIMARY KEY,
    confirm_key varchar(50),
	username varchar(40),
  	email varchar(40) NOT NULL,
    email_confirmed boolean,
  	country varchar(25),
  	pass varchar(256),
	date_join date,
    isAdmin boolean 
);